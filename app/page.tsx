import Link from 'next/link'
import type { ElementType } from 'react'
import { ArrowRight, BookOpen, BrainCircuit, CalendarClock, Heart, Sparkles } from 'lucide-react'
import { getProblems, groupBy } from '@/lib/content'
import { revisionDue } from '@/lib/utils'
import { ProblemCard } from '@/components/problem-card'
export default function Dashboard() { const problems = getProblems(), solved = problems.filter(p => p.status === 'Solved'), due = problems.filter(p => revisionDue(p.revision)); const stats: [string, number, ElementType][] = [['Total Problems', problems.length, BookOpen], ['Solved', solved.length, BrainCircuit], ['Revision Due', due.length, CalendarClock], ['Favorites', problems.filter(p => p.favorite).length, Heart]]; const quote = ['Small, daily repetitions turn hard problems into instinct.', 'Consistency is the compound interest of interview prep.', 'Solve deliberately; review relentlessly.'][new Date().getDate() % 3]; return <div className="shell py-8 pb-24"><section className="rounded-3xl bg-gradient-to-br from-indigo-600 to-violet-700 p-7 text-white shadow-soft sm:p-10"><div className="max-w-2xl"><p className="mb-3 flex items-center gap-2 text-sm font-medium text-indigo-100"><Sparkles className="size-4" />Your daily algorithm practice</p><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">Build recall, one thoughtful problem at a time.</h1><p className="mt-4 text-indigo-100">{quote}</p><div className="mt-6 flex gap-3"><Link href="/problems" className="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-indigo-700">Continue learning</Link><Link href="/admin" className="rounded-xl border border-white/30 px-4 py-2.5 text-sm font-semibold">Create note</Link></div></div></section><section className="mt-6 grid grid-cols-2 gap-3 lg:grid-cols-4">{stats.map(([label, value, Icon]) => <div key={String(label)} className="panel p-5"><Icon className="size-5 text-indigo-500" /><p className="mt-4 text-2xl font-bold">{value}</p><p className="text-sm text-slate-500">{String(label)}</p></div>)}</section><section className="mt-6 grid gap-6 lg:grid-cols-[1.5fr_1fr]"><div className="panel p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Recently solved</h2><p className="mt-1 text-sm text-slate-500">Keep the momentum moving.</p></div><Link href="/problems" className="text-sm text-indigo-600">View all <ArrowRight className="inline size-4" /></Link></div><div className="mt-5 grid gap-3">{solved.slice(0, 3).map(p => <ProblemCard key={p.slug} problem={p} />)}</div></div><aside className="panel p-6"><h2 className="font-semibold">Revision queue</h2><p className="mt-1 text-sm text-slate-500">Spaced repetition for lasting recall.</p><div className="mt-5 space-y-3">{due.length ? due.map(p => <Link key={p.slug} href={`/problems/${p.slug}`} className="block rounded-xl bg-amber-50 p-3 text-sm font-medium text-amber-900 dark:bg-amber-950/50 dark:text-amber-200">{p.title}<span className="float-right text-xs">due today</span></Link>) : <p className="rounded-xl bg-emerald-50 p-4 text-sm text-emerald-700 dark:bg-emerald-950">All caught up. Great work.</p>}</div><h3 className="mt-7 text-sm font-semibold">Top patterns</h3><div className="mt-3 space-y-2">{groupBy(problems, 'pattern').slice(0, 4).map(([name, count]) => <div key={name} className="flex justify-between text-sm"><span>{name}</span><span className="text-slate-500">{count}</span></div>)}</div></aside></section></div> }
