import { getProblems, groupBy } from '@/lib/content'
export const metadata={title:'Statistics'}
const Bar=({label,value,max}:{label:string,value:number,max:number})=><div><div className="mb-1 flex justify-between text-sm"><span>{label}</span><span className="text-slate-500">{value}</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-indigo-500" style={{width:`${Math.max(8,value/max*100)}%`}}/></div></div>
export default function Statistics(){const p=getProblems(), lists=[['Problems by pattern',groupBy(p,'pattern')],['Companies',groupBy(p,'companies')],['Difficulty',groupBy(p,'difficulty')],['Confidence',groupBy(p,'confidence')]];return <div className="shell py-8 pb-24"><h1 className="text-3xl font-bold tracking-tight">Statistics</h1><p className="mt-2 text-slate-500">A clear view of coverage and revision readiness.</p><div className="mt-8 grid gap-5 md:grid-cols-2">{lists.map(([title,data])=>{const rows=data as [string,number][],max=Math.max(...rows.map(x=>x[1]),1);return <section key={String(title)} className="panel p-6"><h2 className="font-semibold">{String(title)}</h2><div className="mt-5 space-y-4">{rows.map(([l,v])=><Bar key={l} label={l} value={v} max={max}/>)}</div></section>})}</div></div>}
