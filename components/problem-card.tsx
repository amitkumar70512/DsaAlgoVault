import Link from 'next/link'
import { CalendarClock } from 'lucide-react'
import type { Problem } from '@/lib/types'
import { cn, dateLabel, revisionDue } from '@/lib/utils'
import { FavoriteButton } from './favorite-button'
const difficulty = { Easy:'bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300', Medium:'bg-amber-50 text-amber-700 dark:bg-amber-950 dark:text-amber-300', Hard:'bg-rose-50 text-rose-700 dark:bg-rose-950 dark:text-rose-300' }
export function ProblemCard({problem:p}:{problem:Problem}) { return <article className="panel group relative p-4 sm:p-5 transition hover:-translate-y-0.5 hover:border-indigo-300 hover:shadow-lg"><div className="absolute right-3 top-3"><FavoriteButton slug={p.slug} initial={p.favorite} compact/></div><Link href={`/problems/${p.slug}`} className="block pr-7"><div><p className="text-xs font-medium text-slate-400">{p.leetcode ? `#${p.leetcode}` : 'Note'}</p><h2 className="mt-1 text-base font-semibold leading-6 group-hover:text-indigo-600">{p.title}</h2></div><div className="mt-4 flex flex-wrap gap-2"><span className={cn('chip border-0',difficulty[p.difficulty])}>{p.difficulty}</span><span className="chip">{p.pattern}</span><span className="chip">{p.datastructure}</span></div></Link><div className="mt-3 flex flex-wrap gap-1.5">{p.tags.map(tag=><Link key={tag} href={`/problems?tag=${encodeURIComponent(tag)}`} className="min-h-8 rounded-md bg-slate-100 px-2 py-1 text-xs leading-6 text-slate-600 hover:bg-indigo-100 hover:text-indigo-700 dark:bg-slate-800 dark:text-slate-300">#{tag}</Link>)}</div><div className="mt-4 flex items-center justify-between gap-2 text-xs text-slate-500"><span className="truncate">{p.companies.slice(0,2).join(' · ')}</span><span className={cn('flex shrink-0 items-center gap-1',revisionDue(p.revision)&&'font-medium text-amber-600')}><CalendarClock className="size-3"/>{revisionDue(p.revision)?'Revision due':dateLabel(p.lastSolved)}</span></div></article> }